'use strict';

const institution = require('./2b-modules.js');
const course_unit = require('./2c-modules.js');

console.log(institution.title, ':', institution.school, '-', institution.year);

console.log(course_unit.title, ':', course_unit.topic, '-', course_unit.section);
